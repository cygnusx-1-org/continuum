<?xml version="1.0" encoding="utf-8"?>
<!--
  A user in Recently Visited. Two trailing toggles in the same order as the Subscriptions Users
  list: follow, then save. Both are hollow/filled rather than appearing and disappearing, so the
  row never reflows.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground">

    <pl.droidsonroids.gif.GifImageView
        android:id="@+id/thing_icon_gif_image_view_item_recently_visited_user"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="center_vertical"
        android:layout_marginEnd="32dp" />

    <TextView
        android:id="@+id/thing_name_text_view_item_recently_visited_user"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="?attr/font_default"
        android:fontFamily="?attr/font_family"
        android:layout_gravity="center_vertical" />

    <ImageView
        android:id="@+id/follow_image_view_item_recently_visited_user"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="end|center_vertical"
        android:layout_marginStart="32dp"
        android:background="?attr/selectableItemBackgroundBorderless" />

    <ImageView
        android:id="@+id/save_image_view_item_recently_visited_user"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="end|center_vertical"
        android:layout_marginStart="16dp"
        android:background="?attr/selectableItemBackgroundBorderless" />

</LinearLayout>
