<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/drag_handle_image_view_item_main_page_tab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:src="@drawable/ic_drag_handle_day_night_24dp"
        android:contentDescription="@string/content_description_drag_to_reorder" />

    <!-- The custom title plus, when renamed, the dimmed computed default to its right — rendered as
         one flowing text so it line-wraps to fit both instead of ellipsizing (see the adapter). -->
    <TextView
        android:id="@+id/name_text_view_item_main_page_tab"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="8dp"
        android:textSize="?attr/font_default"
        android:textColor="?attr/primaryTextColor"
        android:fontFamily="?attr/font_family" />

    <ImageView
        android:id="@+id/delete_image_view_item_main_page_tab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:padding="4dp"
        android:src="@drawable/ic_delete_day_night_24dp"
        android:clickable="true"
        android:focusable="true"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="@string/content_description_delete" />

</LinearLayout>
